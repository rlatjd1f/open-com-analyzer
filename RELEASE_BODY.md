## Open COM Analyzer v0.0.15 릴리즈 노트

Open COM Analyzer v0.0.15 업데이트에서는 요청과 응답을 한눈에 나란히 비교할 수 있는 **TX ↔ RX 연계 패킷 좌우 듀얼 비교 분석기(Dual Packet Inspector)**와 16-bit 정수 및 32-bit 실수가 혼합된 패킷을 자동으로 파싱하는 **지능형 복합 레지스터 디코더**, 그리고 **프로토콜 필드별 고유 색상 식별 체계**가 새롭게 도입되었습니다.

---

### ✨ 신규 기능 (Features)

- **TX ↔ RX 연계 패킷 좌우 듀얼 비교 분석기(Dual Packet Inspector) 탑재**
  - **패킷 페어링 엔진(`packetPairing.ts`) 도입**: Modbus TCP의 Transaction ID(TID), Modbus RTU의 국번/기능코드 및 시간 근접도, 일반 시리얼 프로토콜의 인접 송수신 윈도우를 기반으로 연계된 송수신 패킷을 자동으로 찾아 1:1 매칭합니다.
  - **좌우 2분할 대칭 레이아웃**:
    - **좌측 (요청 패널)**: 요청 패킷의 프로토콜 필드 구조 분석, 원본 바이트 맵, 전송창 바로 넣기 액션 지원
    - **우측 (응답 패널)**: 응답 패킷의 레지스터 페이로드 디코더, 프레임 필드 구조 분석, 표 원클릭 복사 지원
    - 사용자가 보낸 요청(`TX → RX`)이든 외부 장치에서 들어온 요청(`RX → TX`)이든 통신 흐름에 맞춰 요청은 항상 왼쪽, 응답은 항상 오른쪽에 일관되게 정렬됩니다.
  - **RTT 응답 지연시간 뱃지 및 모드 전환**: 상단에 왕복 지연시간(`+X ms`)이 강조 표시되며, `[듀얼 비교]`, `[요청만]`, `[응답만]` 세그먼트 버튼을 통해 원하는 뷰로 손쉽게 전환할 수 있습니다.
  - 연계 패킷이 없는 단독 패킷은 기존의 단일 패킷 인스펙터 뷰로 자연스럽게 표시됩니다.

- **지능형 복합 레지스터 자동 디코더 (Intelligent Auto Mixed Register Decoder)**
  - 산업 현장의 Modbus 패킷에서 상태 코드(16-bit 정수)와 계측값(32-bit Float)이 혼합되어 전송되는 장비들을 지원하기 위해 `⚡ 지능형 자동 판별 (2B 정수 + 4B 실수 혼합)` 모드를 추가했습니다.
  - IEEE-754 Single Precision 부동소수점의 지수 비트 유효 범위, 비정규수, 현실적 계측 범위(NaN/무한대 필터링)를 실시간 휴리스틱 분석하여, 실수 형태의 레지스터 쌍은 4-Byte Float로, 상태값 등은 2-Byte 정수로 지능형 자동 분기 디코딩합니다.

- **패킷 생성기(Packet Builder) 32-bit 정수 및 정수/실수 혼합 샘플 패턴 추가**
  - Modbus 응답 패킷 생성 시 기존의 패턴 외에 **`32-bit 정수`**(`int32`) 및 **`32-bit 정수 / Float 혼합`**(`mixed`) 생성 패턴 옵션이 새롭게 추가되었습니다.
  - 4-Byte 모드뿐만 아니라 2-Byte 모드(레지스터 쌍)에서도 동작하며, `ABCD`, `CDAB`, `DCBA`, `BADC` 엔디안(바이트/워드 순서) 설정이 바이트 스트림에 완벽히 동기화됩니다.

- **프레임 필드 테이블 바이트 길이(Length) 컬럼 추가**
  - 구조 분석 표에 필드별 정확한 크기를 나타내는 `길이` 컬럼을 신설하여 각 부위가 몇 바이트(`1 Byte`, `2 Byte`, `4 Byte` 등)인지 명확하게 파악할 수 있도록 개선했습니다.

- **자주 쓰는 패킷 용어 정비 및 최근 전송 내역 시분초(HH:mm:ss) 타임스탬프 표기**
  - 전송 패널의 버튼 명칭을 '자주 쓰는 패킷'으로 통일하여 용어 일관성을 확보했습니다.
  - 최근 전송 내역 목록에 전송 시각 뱃지(`HH:mm:ss`)를 표기하여 패킷 전송 이력을 직관적으로 확인할 수 있습니다.

---

### 🛠 버그 수정 및 안정성 개선 (Bug Fixes & Stability)

- **듀얼 패킷 분석기 좌우 독립 스크롤 영역 격리**
  - 요청 패널과 응답 패널의 스크롤 컨테이너를 분리하여, 응답 데이터(다량의 레지스터)를 아래로 스크롤할 때도 좌측 요청 패널의 핵심 정보가 상단에 고정 유지되도록 개선했습니다.
- **실시간 패킷 수신 시 선택 탭 유지 보장**
  - '요청만' 또는 '응답만' 탭을 보고 있을 때 백그라운드로 새 패킷이 인입되더라도 듀얼 뷰로 원치 않게 강제 리셋되던 문제를 수정했습니다. (모달 재오픈 시에만 기본 초기화)
- **프레임 필드 구조 분석 표 수평 정렬 일치**
  - 우측 응답 패널의 레지스터 디코더를 구조 분석 표 하단으로 이동 배치하여, 좌우 양측의 프레임 필드 테이블이 동일한 수평선상에서 한눈에 대칭 비교되도록 정렬했습니다.
- **양방향 패킷 페어링 및 응답 매칭 정확도 향상**
  - 순방향/역방향 양방향 검색 및 Modbus 요청/응답 시퀀스 가중치, RTT 시간 근접도 스코어링을 보강하여 오매칭을 방지했습니다.

---

### 🎨 UI/UX 편의성 향상 (UI/UX Improvements)

- **프레임 필드 및 바이트 스트림 맵 전수 고유 컬러링 (Color Distinction)**
  - 트랜잭션 ID, 프로토콜 ID, 길이, 국번, 기능 코드, 레지스터 데이터, CRC 등 모든 프로토콜 필드에 겹치지 않는 고유 인디케이터 색상을 부여했습니다.
  - 하단 원본 바이트 스트림 맵의 각 바이트 블록에도 필드 인디케이터 점과 100% 매칭되는 고유 백그라운드 색상 및 보더를 적용하여 패킷 구조를 시각적으로 즉시 파악할 수 있습니다.
- **긴 HEX 데이터 8바이트 고정 줄바꿈 포맷팅**
  - 긴 레지스터 HEX 데이터가 화면 너비에 따라 글자 단위로 잘리던 현상을 개선하여, 한 줄당 8바이트(16진수 8쌍) 단위로 고정 줄바꿈되도록 구현했습니다.
- **테이블 폰트 확대 및 '설명' 컬럼 정돈**
  - 프레임 필드 표의 폰트 크기를 키우고 상단 정렬(align-top)을 적용하여 시인성을 대폭 높였습니다.
  - 테이블 가독성을 위해 불필요했던 '설명' 컬럼을 제거하고 호버 툴팁(title)으로 대체하여 가로 공간 효율성을 극대화했습니다.
- **모달 가로폭 대폭 확장 및 파싱 값 컬럼 여유 공간 확보**
  - 듀얼 비교 모달의 최대 너비를 `w-[97vw] max-w-[1780px]`로 확장하여 고해상도 모니터에서 시원한 분석 뷰를 제공합니다.
- **서브헤더 액션 버튼 2행 분리 및 불필요한 가로 스크롤바 제거**
  - [HEX 복사], [리포트 복사], [전송창에 적용] 버튼을 하단 2번째 행 좌측 정렬로 이동 배치하여 상단 정보 배지들의 개행 및 가로 스크롤바를 완전히 제거했습니다.
