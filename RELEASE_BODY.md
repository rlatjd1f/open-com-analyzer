## Open COM Analyzer v0.0.14 릴리즈 노트

Open COM Analyzer v0.0.14 업데이트에서는 **IEEE-754 Float32 부동소수점 양방향 변환기**와 **하단 유틸리티 패널 분할 탭(Segmented Controls) UI 개편**, **패킷 분석기 레지스터 번호 및 바이트 오프셋 표기 개선**이 적용되었습니다.

---

### ✨ 신규 기능 (Features)

- **IEEE-754 Float32(32비트 실수) 양방향 진법 변환 지원**
  - 메인 화면 하단 유틸리티 패널 및 패킷 생성기(Packet Builder) 공학용 다중 진법 계산기에 IEEE-754 Float32 변환 기능이 새롭게 탑재되었습니다.
  - HEX 바이트(`3F C0` 또는 `3F C0 00 00`) 입력 시 실수 `1.5`로 즉시 역산되며, 실수 `1.5` 입력 시 표준 HEX 바이트로 자동 상호 변환됩니다.
  - 표준 Big-Endian(`ABCD`) 및 Modicon 계열에서 주로 사용하는 Word-Swap(`CDAB`) 바이트 순서 선택을 지원합니다.
- **ASCII ↔ HEX 및 실수 ↔ HEX 양방향 변환 지원**
  - 메인 하단 유틸리티 3번째 열에서 `HEX → ASCII` 및 `ASCII → HEX` 변환을 지원합니다.
  - 4번째 열에서 `HEX → Float32` 및 `ASCII(실수) → Float32 HEX` 양방향 변환과 원클릭 복사/전송창 적용을 지원합니다.

---

### 🎨 UI/UX 편의성 향상 (UI/UX Improvements)

- **하단 유틸리티 패널 분할 탭(Segmented Controls) 및 토글 인디케이터 UI 개편**
  - 변환기 상단에 `[HEX → ASCII] | [ASCII → HEX]` 및 `[HEX → Float32] | [Float32 → HEX]` 분할 탭 버튼을 적용하여 현재 모드와 전환 가능한 기능을 한눈에 직관적으로 확인할 수 있습니다.
  - `Sum Check`(`HEX / ASCII`) 및 `CRC-16 Check`(`Modbus / CCITT`) 카드 상단에도 동일한 높이와 스타일의 세그먼트 버튼을 적용하여 4개 카드 전체의 시각적 균형과 높이를 일원화했습니다.
  - 입력 안내 행에 `⇄ 전환` 양방향 아이콘 버튼을 추가하여 토글 인터랙션을 명확히 하였습니다.
- **패킷 분석기(Packet Inspector) 레지스터 번호 1:1 매핑 개선**
  - 사용자가 선택한 단위 크기(2B / 4B / 8B)에 맞춰 레지스터 번호가 `Reg #0..#1` 대신 `Reg #0`, `Reg #1`, `Reg #2`... 로 단일 레지스터 번호로 직관적 카운트되도록 정돈되었습니다.
- **패킷 분석기 바이트 오프셋 `[시작~끝]` 대괄호 물결표(`~`) 포맷 적용**
  - 불필요한 'B' 접미사를 제거하고 `[0~3]`, `[4~7]` 형태의 익숙하고 깔끔한 인덱스 범위 표기로 시인성을 대폭 향상했습니다.
- **패킷 생성기 공학용 계산기 초기 상태 클린업**
  - 모달을 열었을 때 테스트용 기본값이 남아있지 않도록 깔끔한 빈 입력 상태(placeholder 안내)로 초기화됩니다.
